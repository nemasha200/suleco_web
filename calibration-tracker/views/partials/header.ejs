<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title : 'Calibration Tracker' %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<div class="topbar">
  <div class="brand">Calibration <span>Tracker</span></div>
  <nav>
    <a href="/">Dashboard</a>
    <a href="/customers">Customers</a>
    <a href="/equipment/new">Add Equipment</a>
    <a href="/export.csv">Export CSV</a>
    <form method="POST" action="/logout" style="display:inline">
      <button type="submit" class="linklike">Logout (<%= typeof username !== 'undefined' ? username : '' %>)</button>
    </form>
  </nav>
</div>
<div class="container">
